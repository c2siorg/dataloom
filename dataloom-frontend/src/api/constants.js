/**
 * API constants for DataLoom.
 * @module api/constants
 */

/**
 * Default number of rows per page for paginated API responses.
 * Matches the backend default.
 */
export const DEFAULT_PAGE_SIZE = 50;
