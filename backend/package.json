{
  "name": "backend",
  "version": "1.0.0",
  "scripts": {
    "dev": "./env/bin/uvicorn app.main:app --reload",
    "start": "./env/bin/uvicorn app.main:app",
    "build": "pip install -r requirements.txt"
  }
}